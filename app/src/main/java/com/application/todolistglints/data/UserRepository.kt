package com.application.todolistglints.data

import android.content.Context
import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import com.application.todolistglints.utils.DataDummy

class UserRepository {
/*    companion object {
        private var loginRepository: UserRepository? = null

        fun getInstance(context: Context): UserRepository {
            if (loginRepository == null) {
                loginRepository = UserRepository()
            }
            return loginRepository!!
        }
    }

    fun login(email: String, password: String): LiveData<UserEntity> {
        val loginData = MutableLiveData<UserEntity>()
        val user = DataDummy.generateDummyUser()
        loginData.value = user
    }*/
}